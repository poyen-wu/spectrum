import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: { main: "#7cc4ff" },
    secondary: { main: "#ffd27c" }
  },
  shape: { borderRadius: 12 }
});
